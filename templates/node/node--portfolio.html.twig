{% extends "node.html.twig" %}
{% set container = theme.settings.fluid_container ? 'container-fluid' : 'container' %}
{# Content #}
{% block content %}
<div class="container-sm">
  <div class="page-header">
    <h1>
      <small>{{'Portfolio'|t}} : </small>
      <br/>
      {{label}}
    </h1>
  </div>
  <div{{ content_attributes.addClass('content') }}>
    {{ content }}
  </div>
</div>
{% endblock %}
